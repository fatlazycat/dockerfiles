consul1:
  image: "fatlazycat/consul:latest"
  container_name: "consul1"
  hostname: "consul1"
  ports:
    - "8080:8080"
  volumes:
    - ~/Development/Environment/Consul/config/server:/config
    - ~/Development/Environment/Consul/ssl:/ssl
  command: "agent -config-dir /config -bootstrap"
